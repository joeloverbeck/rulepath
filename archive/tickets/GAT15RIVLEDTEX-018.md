# GAT15RIVLEDTEX-018: Web e2e no-leak smoke, CI registration, and catalog-docs reconciliation

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: Yes (presentation-only / test infra) — `apps/web/e2e/river-ledger.smoke.mjs`, `apps/web/package.json`, `ci/games.json`, `apps/web/README.md`, `README.md`, `apps/web/public/rules/manifest.json`
**Deps**: GAT15RIVLEDTEX-017

## Problem

River Ledger needs a browser e2e smoke proving observer and wrong-seat DOM/storage/log no-leak plus legal-only controls, and the catalog-doc/CI surfaces (`ci/games.json`, `smoke:e2e`, README catalog lists, rules manifest) must be reconciled so the gate's continuous checkers go green. This is the web verification capstone.

## Assumption Reassessment (2026-06-14)

1. `apps/web/e2e/poker-lite.smoke.mjs` is the precedent; `apps/web/package.json` `smoke:e2e` chains per-game smokes; `ci/games.json` rows (`id`/`sim_flags`/`e2e`) are validated by `scripts/check-ci-games.mjs` (games/ ↔ manifest set equality + e2e file existence); `scripts/check-catalog-docs.mjs` is data-driven off the `crates/wasm-api` catalog const; `apps/web/public/rules/manifest.json` is generated by `scripts/copy-player-rules.mjs` (verified).
2. `specs/...-base.md` §4.3 (web/e2e rows), §10.5/§10.6, and §6 exit row 5 fix the smoke + catalog surfaces.
3. Cross-artifact boundary under audit: `ci/games.json` gains a `river_ledger` row whose `e2e` names `river-ledger.smoke.mjs` created here (co-land so `check-ci-games` passes); the README catalog surfaces and `manifest.json` key off the WASM const added in 016 — this ticket closes the expected `check-catalog-docs`/`check-ci-games` red window opened when `games/river_ledger/` (003) and the WASM const (016) landed.
4. FOUNDATIONS §11 no-leak (browser surfaces) motivates this ticket: the e2e asserts observer and wrong-seat viewers receive no hole/burn/deck-tail fact in DOM attributes, local/session storage, console logs, or test snapshots.
5. §11 no-leak firewall enforcement surface (browser) under audit: the e2e is the end-to-end proof that the Rust-side firewall holds through the WASM bridge and DOM; confirm observer + wrong-seat negative assertions cover hole/burn/deck-tail/future-community facts.

## Architecture Check

1. Co-landing `ci/games.json` + the e2e file + the catalog-doc surfaces in one ticket avoids a multi-PR red-CI window and makes the web verification a single reviewable capstone (Deliverable-doubles-as-capstone).
2. No backwards-compatibility aliasing/shims — additive smoke + catalog rows.
3. `engine-core` untouched (§3); no `game-stdlib` change (§4); no legality in TypeScript (§2).

## Verification Layers

1. Observer + wrong-seat DOM/storage/log no-leak + legal-only controls -> `npm --prefix apps/web run smoke:e2e` (incl. `river-ledger.smoke.mjs`).
2. `ci/games.json` ↔ `games/` in sync with a valid e2e file -> `node scripts/check-ci-games.mjs`.
3. README catalog surfaces + rules manifest match the WASM catalog -> `node scripts/check-catalog-docs.mjs`.
4. Presentation copy stays neutral -> `node scripts/check-presentation-copy.mjs`.

## What to Change

### 1. E2E smoke + CI registration

Author `apps/web/e2e/river-ledger.smoke.mjs` (seat order, active/pending, viewer modes, legal-only controls, safe previews, final breakdown, observer + wrong-seat DOM/storage/log no-leak); add it to `smoke:e2e` in `apps/web/package.json`; add the `river_ledger` row to `ci/games.json`.

### 2. Catalog-doc reconciliation

Add River Ledger to the `apps/web/README.md` intro catalog list, Shell Surface renderer list, action-presentation audit, effect-animation audit, `smoke:e2e` list, and no-leak/a11y checklist; add it to the root `README.md` "current official games are" list; regenerate `apps/web/public/rules/manifest.json` via `scripts/copy-player-rules.mjs`.

## Files to Touch

- `apps/web/e2e/river-ledger.smoke.mjs` (new)
- `apps/web/package.json` (modify — `smoke:e2e`)
- `ci/games.json` (modify)
- `apps/web/README.md` (modify)
- `README.md` (modify)
- `apps/web/public/rules/manifest.json` (modify — regenerated)

## Out of Scope

- WASM registration + player-rules docs (GAT15RIVLEDTEX-016).
- Renderer component (GAT15RIVLEDTEX-017).
- Trailing game docs + final atlas review + gate close (019/020/021).

## Acceptance Criteria

### Tests That Must Pass

1. `npm --prefix apps/web run build && npm --prefix apps/web run smoke:e2e` — River Ledger smoke passes incl. observer/wrong-seat no-leak.
2. `node scripts/check-ci-games.mjs` and `node scripts/check-catalog-docs.mjs` — pass (red window closed).
3. `node scripts/check-presentation-copy.mjs` — passes.

### Invariants

1. No hidden fact reaches DOM/storage/logs/snapshots for observer or wrong-seat viewers (§11).
2. Controls are legal-only; visuals are neutral and original (§7/§10).

## Test Plan

### New/Modified Tests

1. `apps/web/e2e/river-ledger.smoke.mjs` (new) — browser no-leak + legal-only + outcome smoke.
2. `ci/games.json` (modify) — River Ledger CI discovery row.

### Commands

1. `npm --prefix apps/web run build && npm --prefix apps/web run smoke:e2e`
2. `node scripts/check-ci-games.mjs && node scripts/check-catalog-docs.mjs && node scripts/check-presentation-copy.mjs`
3. These cover the browser + catalog-doc boundary; full Rust acceptance is swept in GAT15RIVLEDTEX-021.

## Outcome

Completed 2026-06-14.

- Added `apps/web/e2e/river-ledger.smoke.mjs`, covering six-seat setup, observer and wrong-seat no-leak surfaces, storage/log checks, legal-only action buttons, terminal outcome rendering, and responsive layout.
- Chained `river-ledger.smoke.mjs` into `apps/web` `smoke:e2e`.
- Added `river_ledger` to `ci/games.json` with the River Ledger e2e file and seat-count simulator flags.
- Updated root and web README catalog surfaces, web renderer/action/animation audit rows, smoke list, and the no-leak/a11y checklist.
- Regenerated `apps/web/public/rules/manifest.json` through `scripts/copy-player-rules.mjs`; no source rules Markdown changed.
- Updated `rules-display.smoke.mjs` expected game list for the 15-game catalog.

Verification passed:

1. `npm --prefix apps/web run build`
2. `node apps/web/e2e/river-ledger.smoke.mjs` (rerun with localhost/Chrome escalation after sandbox `listen EPERM`)
3. `node apps/web/e2e/rules-display.smoke.mjs`
4. `npm --prefix apps/web run smoke:e2e`
5. `node scripts/check-ci-games.mjs`
6. `node scripts/check-catalog-docs.mjs`
7. `node scripts/check-player-rules.mjs`
8. `node scripts/check-presentation-copy.mjs`
9. `node scripts/check-doc-links.mjs`
10. `git diff --check`
